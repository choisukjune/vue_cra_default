apiVersion: v1
kind: Service
metadata:
  labels:
    app: vue.cra
  name: vue.cra
spec:
  ports:
    - name: 3000-3000
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: vue.cra
  type: LoadBalancer